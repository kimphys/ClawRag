"""
Authentication endpoints.

NOTE: This is a placeholder. The actual authentication logic is bypassed
by the dummy `get_current_user` dependency for the beta version.
"""

from fastapi import APIRouter

router = APIRouter()