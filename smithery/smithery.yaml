startCommand:
  type: stdio
  config:
    command: "npx"
    args:
      - "-y"
      - "@clawrag/mcp-server"
  environmentVariables:
    CLAWRAG_API_URL:
      type: string
      description: "URL of the running ClawRAG backend (default: http://localhost:8080)"
      required: false
      default: "http://localhost:8080"
    CLAWRAG_TIMEOUT:
      type: number
      description: "Timeout for API requests in milliseconds (default: 120000)"
      required: false
      default: 120000
    LOG_LEVEL:
      type: string
      description: "Logging level (DEBUG, INFO, WARN, ERROR) (default: INFO)"
      required: false
      default: "INFO"